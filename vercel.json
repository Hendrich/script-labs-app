{
  "builds": [
    {
      "src": "frontend-react/vite.config.js",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "frontend-react/dist"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
